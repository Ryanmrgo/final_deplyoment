services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    env_file:
      - .env.local        # loads all your secrets at runtime
    environment:
      - NODE_ENV=production
    restart: unless-stopped
